<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.vogella.eclipse.mcp</groupId>
  <artifactId>com.vogella.eclipse.mcp.parent</artifactId>
  <version>0.2.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Eclipse MCP Server</name>

  <properties>
    <tycho.version>5.0.3</tycho.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <modules>
    <module>plugins</module>
    <module>features</module>
    <module>tests</module>
    <module>update-site</module>
  </modules>

  <build>
    <plugins>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-maven-plugin</artifactId>
        <version>${tycho.version}</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>target-platform-configuration</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <target>
            <file>${maven.multiModuleProjectDirectory}/target-platform/com.vogella.eclipse.mcp.target/com.vogella.eclipse.mcp.target.target</file>
          </target>
          <executionEnvironment>JavaSE-25</executionEnvironment>
          <environments>
            <environment><os>linux</os><ws>gtk</ws><arch>x86_64</arch></environment>
            <!-- the build server is linux aarch64. With includeAllPlatforms false in
                 the target, an environment that is not listed here gets no SWT
                 fragment, and tycho-surefire then cannot provision a test runtime for
                 the machine the build is running on -->
            <environment><os>linux</os><ws>gtk</ws><arch>aarch64</arch></environment>
            <environment><os>win32</os><ws>win32</ws><arch>x86_64</arch></environment>
            <environment><os>macosx</os><ws>cocoa</ws><arch>aarch64</arch></environment>
          </environments>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-surefire-plugin</artifactId>
        <version>${tycho.version}</version>
        <configuration>
          <useUIHarness>false</useUIHarness>
          <!-- a build that runs no tests must fail: with this false, Tycho silently
               picking the junit4 provider made every build green while discovering
               none of the JUnit 5 tests -->
          <failIfNoTests>true</failIfNoTests>
          <providerHint>junit5</providerHint>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
